using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Devour : Skills
{
    
    // Start is called before the first frame update
    void Start()
    {
        SkillName = "Devour";
        SkillDescription = "Consume Domain tiles";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
